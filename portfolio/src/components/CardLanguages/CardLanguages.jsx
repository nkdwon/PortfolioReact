import styles from './CardLanguages.module.css'

function CardLanguages() {

 /*  const [ repositories, setRepositories ] = useState([])

  const languages = Object.keys(data)

  useEffect(() => {
      const buscarRepositorios = async () => {
          const response = await fetch('https://api.github.com/users/nkdwon/repos')
          const data = await response.json()
          setRepositories(data)
      }
      buscarRepositorios()
  }, [])

  `https://api.github.com/repos/nkdwon/`{$nome_do_repositorio}`/languages` */

  return (
    <div className={styles.card_icones}>
      
    </div>
  )
}

export default CardLanguages